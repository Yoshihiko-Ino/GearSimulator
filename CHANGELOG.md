# 変更履歴

## v2.0.0

### 試算DPSとFFLogs連携

- 試算DPSを、保存したログ取得時装備を基準に評価する方式へ更新しました。
- FFLogsのSpell／Weaponskill実測値から通常回転のGCDを推定し、同じGCD段階を維持した装備を探索できるようにしました。
- 詠唱開始・完了の重複、アビリティ、ダウンタイム、短周期バーストGCDを推定対象から分離しました。
- raid基準と本人基準のログ評価を維持し、ログ・ファイト・アクター・基準装備が一致する場合だけ保存値を利用します。
- FFLogsの集計内容、有効戦闘時間、基準装備、計算モデルが変わった保存スコアは「要再計算」と表示します。

### 装備探索の精度

- 絶コンテンツのレベル・IL制限を考慮した候補判定を追加しました。
- Resistance Weapon、Manderville Weapon、Phantom Weaponのカスタムサブステータスを装備探索へ反映しました。
- ILシンク後の同一ステータス装備を安全にまとめ、シンク前のILだけで有効候補を除外しないようにしました。
- 装備候補の暫定順位を最終条件で再評価し、低スコア装備や空マテリア構成が誤って反映される問題を修正しました。
- 上位候補になり得る数学的上限を使い、最良候補と同率候補を保持したまま不要な完全計算を省くようにしました。

### 食事

- 食事を「食事なし（固定）」「食事をシミュレーション」「特定食事（固定）」に分けました。
- 食事はコンテンツILへシンクされず、シンク後の装備ステータスへ食事固有の割合と上限が適用される方式に修正しました。
- ジョブに有効な最高IL帯の戦闘食を比較し、Tenacity、Piety、Skill Speed、Spell Speedを含む候補を評価します。
- 他の食事に常に劣る候補だけを除外し、有効な食事候補を保持します。

### 速度と操作性

- 装備候補の完全評価とプレビュー評価を複数CPUプロセスへ分配しました。
- CPU並列数を自動または1／2／4／6／8／12／16から選択できるようにしました。
- 重複するマテリア構成、食事計算、GCD派生値、候補再評価を再利用し、探索時間を短縮しました。
- 通常探索、既知装備評価、片側固定再探索で計算結果とプロセスを再利用するようにしました。
- 探索段階、処理件数、経過時間、推定残り時間を進捗表示へ追加しました。
- 処理完了後はキャンセルボタンを非表示にし、完了状態を明確にしました。

### 保存セットと装備編集

- 保存セットへ計算条件と検証情報を保存し、古い異常値を現在のスコアとして表示しないようにしました。
- 「要再計算」のセットを選択または「計算」した場合に再評価し、再計算中・ログ待機中・完了・保存不可を表示します。
- ロック中でも装備構成を維持したまま、派生ステータスとスコア検証情報を更新できるようにしました。
- 各部位の除外数を左クリックすると除外装備一覧を表示し、一覧内の装備を右クリックすると除外を解除できるようにしました。
- 特殊武器を含む装備一覧をIL順に表示するよう修正しました。
- XIVGear／BisBuddyへの入出力、圧縮候補の具体装備選択、除外状態の表示を改善しました。

### 安定性

- 計算開始、完了、取消、エラー時のUI復旧を強化しました。
- 並列計算の起動に失敗した場合は未完了候補を直列計算へ切り替えるようにしました。
- 設定・保存セット・キャッシュの保存を安全化し、破損ファイルを退避して起動を継続できるようにしました。
- 保存したFFLogsクライアントシークレットをWindowsの暗号化機能で保護します。

## v1.0.8

### 安定性とデータ保護

- v1.0.7以前の設定と保存セットを、新しいフォルダの設定が空の場合だけ引き継ぐようにしました。
- 一度移行した設定を再移行せず、利用者が削除した認証情報や保存セットが復活しないようにしました。
- 設定、保存セット、キャッシュを一時ファイル経由で保存し、書き込み中断による破損を防ぐようにしました。
- 破損したJSONを別名で退避し、安全な初期状態で起動できるようにしました。
- FFLogsキャッシュへ有効期限と強制再取得を追加しました。
- 終了時に実行中処理を待機し、保存中や通信中の終了による破損を防ぐようにしました。

### 装備探索

- 装備探索の重複処理と不要な一時データを削減し、候補数を維持しながら速度とメモリ使用量を改善しました。
- レベルシンク時に有効な中間IL装備を残し、シンク後ステータスが同一上限となる装備だけをまとめるようにしました。
- Resistance WeaponとManderville Weaponのカスタムステータス候補を追加しました。
- 装備固定、マテリア固定、特殊武器補正、左右装備の再探索を最終結果まで維持するようにしました。
- 食事候補のIL、ジョブ関連ステータス、Tenacity、Piety、速度ステータスの判定を修正しました。

### 操作性

- 装備データの強制再取得で、選択ジョブの装備一覧も更新するようにしました。
- 保存セット計算中の画面停止、進捗表示、キャンセル表示、ジョブ切り替え時の表示不整合を修正しました。
- 装備一覧の圧縮表示、指輪表示、除外状態、特殊武器表示を改善しました。

## v1.0.7以前

過去の変更内容は各リリースの説明を参照してください。
