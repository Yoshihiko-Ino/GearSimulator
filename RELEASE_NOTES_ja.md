# GearSimulator v2.0.1

## 主な変更

- 新しいバージョンがある場合だけ通知する自動アップデート確認を追加しました。Windows単体版は配布ファイルを検証してから更新し、失敗時は旧EXEへ戻します。
- 過去30件のFFLogsイベントを圧縮保存し、同じログを再表示する際のAPI取得を省くようにしました。
- XIVAPIのAction／Statusを一括取得し、同時通信数制限、HTTP 429再試行、404の一時保存を追加しました。
- 使用中のXIVAPIデータだけを定期的に再検証し、不要なAction／Status通信を削減しました。
- マテリアのexact探索とFFLogsダメージ計算を軽量化し、探索精度を維持したまま計算時間とメモリ使用量を削減しました。
- FFLogs認証期限切れ時の再試行、ログ基準装備の復元失敗時の保護、API応答・更新処理の検証を強化しました。

## v1.0.8以降の変更履歴

機能別の詳細は[CHANGELOG.md](CHANGELOG.md)を参照してください。

## 更新時の注意

- 旧バージョンを上書きせず、v2.0.1を別のフォルダへ配置してください。
- 旧バージョンと同じ親フォルダへ配置すると、初回起動時に設定と保存セットを引き継げます。
- Windows単体版の自動更新では、GitHubから取得したEXEのサイズとSHA-256を確認してから差し替えます。

## 配布ファイル

- `GearSimulator.exe`
- `GearSimulator_v2.0.1_py.zip`
- `SHA256SUMS.txt`
