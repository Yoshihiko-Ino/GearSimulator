# GearSimulator v2.0.0

## 主な変更

- FFLogsの実測GCDを使った試算DPS装備探索に対応しました。
- 絶コンテンツのレベル・IL制限と、Resistance／Manderville／Phantom Weaponのカスタムステータスを探索へ反映しました。
- 食事なし、食事シミュレーション、特定食事固定を明確に分離しました。
- 装備候補の評価を複数CPUプロセスへ分配し、探索時間を短縮しました。
- 精度を維持できる候補上限を使い、最良候補にならない完全計算を省くようにしました。
- 保存スコアへ計算条件の検証を追加し、古い値や条件の異なる値は「要再計算」と表示します。
- 除外装備一覧の表示と個別解除、装備IL順、進捗・キャンセル表示を改善しました。
- 設定・保存セット・キャッシュの破損対策と、異常終了時の復旧を強化しました。

## v1.0.8以降の変更履歴

機能別の詳細は[CHANGELOG.md](CHANGELOG.md)を参照してください。

## 更新時の注意

- 旧バージョンを上書きせず、v2.0.0を別のフォルダへ配置してください。
- 旧バージョンと同じ親フォルダへ配置すると、初回起動時に設定と保存セットを引き継げます。
- 旧形式の保存スコアは装備データを保持したまま「要再計算」になります。
- 試算DPSの再計算には、対象のFFLogsファイトとアクターを選択して「キャスト取得」を実行する必要があります。

## 配布ファイル

- `GearSimulator.exe`
- `GearSimulator_v2.0.0_py.zip`
- `SHA256SUMS.txt`
